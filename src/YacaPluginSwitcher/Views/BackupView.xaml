<UserControl x:Class="YacaPluginSwitcher.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="BackupActionButtonStyle" BasedOn="{StaticResource NormalActionButtonStyle}" TargetType="Button"><Setter Property="MinWidth" Value="150" /><Setter Property="Height" Value="40" /></Style>
        <Style x:Key="DeleteBackupButtonStyle" BasedOn="{StaticResource DeleteActionButtonStyle}" TargetType="Button"><Setter Property="MinWidth" Value="150" /><Setter Property="Height" Value="40" /></Style>
        <ControlTemplate x:Key="TransparentDataGridCellTemplate" TargetType="DataGridCell">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" ContentStringFormat="{TemplateBinding ContentStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}" />
            </Border>
        </ControlTemplate>
        <Style x:Key="CenteredCellStyle" TargetType="DataGridCell">
            <Setter Property="Template" Value="{StaticResource TransparentDataGridCellTemplate}" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" /><Setter Property="Background" Value="Transparent" /><Setter Property="BorderBrush" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /><Setter Property="Padding" Value="8,0" /><Setter Property="HorizontalContentAlignment" Value="Center" /><Setter Property="VerticalContentAlignment" Value="Center" /><Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
        <Style x:Key="CenteredTextStyle" TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" /><Setter Property="HorizontalAlignment" Value="Center" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /></Style>
        <Style x:Key="CenteredHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource ControlBrush}" /><Setter Property="Foreground" Value="{StaticResource AccentBrush}" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="HorizontalContentAlignment" Value="Center" /><Setter Property="VerticalContentAlignment" Value="Center" /><Setter Property="Padding" Value="8,0" /><Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" /><Setter Property="BorderThickness" Value="0,0,0,1" /><Setter Property="SeparatorVisibility" Value="Collapsed" />
        </Style>
        <ControlTemplate x:Key="BackupSelectionCheckBoxTemplate" TargetType="CheckBox">
            <Grid Width="18" Height="18" Background="{TemplateBinding Background}">
                <Border x:Name="CheckBorder" Background="{TemplateBinding Background}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <Path x:Name="CheckMark" Data="M 3,9 L 7,13 L 15,4" Stroke="{StaticResource GoldBrush}" StrokeThickness="2" Fill="Transparent" Visibility="Collapsed" Stretch="None" />
                </Border>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True"><Setter TargetName="CheckMark" Property="Visibility" Value="Visible" /><Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource GoldBrush}" /></Trigger>
                <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CheckBorder" Property="Background" Value="{StaticResource ControlHoverBrush}" /><Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource GoldBrush}" /></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45" /></Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <Style x:Key="BackupSelectionCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Template" Value="{StaticResource BackupSelectionCheckBoxTemplate}" />
            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Background}" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="HorizontalAlignment" Value="Center" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Style.Triggers><DataTrigger Binding="{Binding CanSelect}" Value="False"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers>
        </Style>
        <DataTemplate x:Key="PluginDownloadsVersionHeaderTemplate">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="18" /></Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource AccentBrush}" TextAlignment="Center" />
                <Button Grid.Row="1" Width="18" Height="18" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Foreground="{StaticResource GoldBrush}"
                        ToolTip="Sortierung umschalten" Click="PluginDownloadsSort_Click" Style="{StaticResource HeaderSortButtonStyle}">
                    <Path Width="14" Height="14" Stretch="Uniform" Fill="{StaticResource GoldBrush}"
                          Data="M 7,1 L 7,10 M 3,4 L 7,1 L 11,4 M 3,10 L 7,13 L 11,10" />
                </Button>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="6,6,0,0">
        <Grid.RowDefinitions><RowDefinition Height="58" /><RowDefinition Height="1" /><RowDefinition Height="*" /><RowDefinition Height="58" /></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,6,0">
            <TextBlock x:Name="PageHeaderText" Text="BACKUPS VERWALTEN" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" />
            <TextBlock x:Name="TitleText" Visibility="Collapsed" />
            <TextBlock x:Name="BackupCapacityText" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource AccentBrush}" FontSize="28" FontWeight="SemiBold" />
        </Grid>
        <Border Grid.Row="1" Margin="0,0,6,0" Background="{StaticResource AccentBrush}" Opacity="0.65" />

        <Grid Grid.Row="2">
            <Grid.RowDefinitions><RowDefinition x:Name="BackupPanelRow" Height="Auto" /><RowDefinition Height="12" /><RowDefinition x:Name="PluginDownloadsPanelRow" Height="*" /></Grid.RowDefinitions>
            <Border x:Name="BackupCard" Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="0" Padding="10">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="38" /><RowDefinition Height="1" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="4,0,0,0">
                        <TextBlock x:Name="BackupSectionHeader" Text="YACA Plugin Backups" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                        <TextBlock x:Name="BackupReplacementText" FontSize="13" Foreground="{StaticResource SecondaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="8,0,4,0" TextAlignment="Right" />
                    </Grid>
                    <Border Grid.Row="1" Background="{StaticResource AccentSoftBrush}" Opacity="0.65" />
                    <DataGrid x:Name="Grid" AlternationCount="2" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserResizeRows="False" CanUserSortColumns="False" IsReadOnly="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource ForegroundBrush}" BorderThickness="0" SelectionMode="Single" RowHeight="44" ColumnHeaderHeight="38" ColumnHeaderStyle="{StaticResource CenteredHeaderStyle}" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <DataGrid.Resources><SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" /><SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" /></DataGrid.Resources>
                        <DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="Background" Value="{StaticResource SurfaceBrush}" /><Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" /><Setter Property="VerticalContentAlignment" Value="Center" /><Style.Triggers><Trigger Property="ItemsControl.AlternationIndex" Value="1"><Setter Property="Background" Value="{StaticResource AlternatingRowBrush}" /></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource ControlHoverBrush}" /></Trigger><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{StaticResource NavSelectedBrush}" /></Trigger></Style.Triggers></Style></DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="52"><DataGridTemplateColumn.CellTemplate><DataTemplate><CheckBox IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Checked="BackupSelection_Changed" Unchecked="BackupSelection_Changed" Style="{StaticResource BackupSelectionCheckBoxStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                            <DataGridTextColumn Header="Backup" Binding="{Binding DisplayName}" Width="220" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                            <DataGridTextColumn Header="Datum" Binding="{Binding Timestamp, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="180" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                            <DataGridTextColumn Header="Größe" Binding="{Binding FileSizeDisplay}" Width="120" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                            <DataGridTextColumn Header="SHA-256" Binding="{Binding Sha256}" Width="470" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border x:Name="PluginDownloadsCard" Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource GoldBrush}" BorderThickness="1" CornerRadius="0" Padding="10" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="38" /><RowDefinition Height="1" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="4,0,0,0">
                        <TextBlock x:Name="PluginDownloadsTitle" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="{StaticResource GoldBrush}" />
                    </Grid>
                    <Border Grid.Row="1" Background="{StaticResource GoldBrush}" Opacity="0.65" />
                    <DataGrid x:Name="PluginDownloadsGrid" AlternationCount="2" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserResizeRows="False" CanUserSortColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource ForegroundBrush}" BorderThickness="0" SelectionMode="Single" RowHeight="44" ColumnHeaderHeight="38" ColumnHeaderStyle="{StaticResource CenteredHeaderStyle}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <DataGrid.Resources><SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" /><SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" /></DataGrid.Resources>
                        <DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="Background" Value="{StaticResource SurfaceBrush}" /><Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" /><Setter Property="VerticalContentAlignment" Value="Center" /><Style.Triggers><Trigger Property="ItemsControl.AlternationIndex" Value="1"><Setter Property="Background" Value="{StaticResource AlternatingRowBrush}" /></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource ControlHoverBrush}" /></Trigger><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{StaticResource NavSelectedBrush}" /></Trigger></Style.Triggers></Style></DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="52"><DataGridTemplateColumn.CellTemplate><DataTemplate><CheckBox IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Checked="PluginDownloadSelection_Changed" Unchecked="PluginDownloadSelection_Changed" Style="{StaticResource BackupSelectionCheckBoxStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                            <DataGridTextColumn Header="Datei" Binding="{Binding FileName}" Width="220" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                            <DataGridTextColumn Header="Datum" Binding="{Binding Timestamp, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="180" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                            <DataGridTextColumn Header="Größe" Binding="{Binding FileSizeDisplay}" Width="120" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                            <DataGridTextColumn Header="Version" HeaderTemplate="{StaticResource PluginDownloadsVersionHeaderTemplate}" Binding="{Binding Version}" Width="140" ElementStyle="{StaticResource CenteredTextStyle}" CellStyle="{StaticResource CenteredCellStyle}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="6,6,6,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="1*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="1*" /></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center"><Button x:Name="RestoreButton" Content="Wiederherstellen" Click="Restore_Click" Margin="6,0" Style="{StaticResource BackupActionButtonStyle}" /><Button x:Name="CloseButton" Content="Schließen" Click="Close_Click" Margin="6,0" Style="{StaticResource BackupActionButtonStyle}" /></StackPanel>
            <Button x:Name="DeleteButton" Grid.Column="2" Content="LÖSCHEN" Click="Delete_Click" HorizontalAlignment="Right" Margin="6,0,0,0" Style="{StaticResource DeleteBackupButtonStyle}" />
        </Grid>
    </Grid>
</UserControl>
