<UserControl x:Class="YacaPluginSwitcher.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Zentraler Seitenheader für die Konfigurationsseite. -->
            <StackPanel Grid.Row="0" Margin="0,4,0,14">
                <TextBlock x:Name="TitleText"
                           Text="KONFIGURATION"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource GoldBrush}" />
                <Border Height="1"
                        Margin="0,8,0,0"
                        Background="{StaticResource AccentSoftBrush}" />
            </StackPanel>

            <!-- Allgemeine Einstellungen -->
            <Border Grid.Row="1"
                    Style="{StaticResource CardStyle}"
                    Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="ALLGEMEIN"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}"
                               Margin="0,0,0,10" />

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Sprache"
                                   FontSize="14"
                                   VerticalAlignment="Center" />
                        <ComboBox x:Name="LanguageCombo"
                                  Grid.Column="1"
                                  Height="36"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="Language_SelectionChanged"
                                  PreviewMouseLeftButtonDown="LanguageCombo_PreviewMouseLeftButtonDown" />
                    </Grid>

                    <CheckBox x:Name="AutomaticBackup"
                              Margin="0,5" />
                    <CheckBox x:Name="WarnRunning"
                              Margin="0,5" />
                    <CheckBox x:Name="KeepYacaPluginDownloads"
                              Margin="0,5" />
                    <CheckBox x:Name="DownloadAllWithoutPrompt"
                              Margin="0,5" />

                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Maximale Backups"
                                   FontSize="14"
                                   VerticalAlignment="Center" />
                        <TextBox x:Name="MaxBackups"
                                 Grid.Column="1"
                                 Width="100"
                                 Height="34"
                                 HorizontalAlignment="Left"
                                 VerticalContentAlignment="Center" />
                    </Grid>

                    <TextBlock Text="Aktiver TeamSpeak-Pfad"
                               Margin="0,12,0,5"
                               FontSize="14"
                               Foreground="{StaticResource SecondaryBrush}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ActivePath"
                                 Height="36"
                                 HorizontalAlignment="Stretch"
                                 VerticalContentAlignment="Center"
                                 ToolTip="Vollständiger TeamSpeak Plugin-Pfad" />
                        <Button Grid.Column="1"
                                Content="Durchsuchen"
                                Click="Browse_Click"
                                Width="120"
                                Height="40"
                                Margin="8,0,0,0"
                                Style="{StaticResource NormalActionButtonStyle}" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Erweiterte Einstellungen -->
            <Border Grid.Row="2"
                    Style="{StaticResource CardStyle}"
                    Margin="0,0,0,10">
                <StackPanel>
                    <CheckBox x:Name="Expert"
                              Content="Experteneinstellungen"
                              FontSize="15"
                              FontWeight="SemiBold"
                              IsChecked="False"
                              Checked="Expert_Changed"
                              Unchecked="Expert_Changed"
                              Margin="0,0,0,8" />

                    <StackPanel x:Name="ExpertPanel"
                                Visibility="Collapsed">
                        <Border Background="{StaticResource ControlBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                Padding="14"
                                Margin="0,2,0,10">
                            <StackPanel>
                                <TextBlock Text="TEAMSPEAK INSTANZEN"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource GoldBrush}"
                                           Margin="0,0,0,8" />
                                <CheckBox x:Name="MultipleInstances"
                                          Margin="0,3,0,8" />
                                <TextBlock Text="Verfügbare TeamSpeak-Pfade"
                                           FontSize="14"
                                           Foreground="{StaticResource SecondaryBrush}"
                                           Margin="0,2,0,5" />
                                <ListBox x:Name="PathsList"
                                         Height="130"
                                         Background="{StaticResource BackgroundBrush}"
                                         BorderBrush="{StaticResource BorderBrush}"
                                         BorderThickness="1"
                                         FontSize="14"
                                         Padding="4" />
                                <WrapPanel Margin="0,8,0,0">
                                    <Button Content="Hinzufügen"
                                            Click="AddPath_Click"
                                            Style="{StaticResource NormalActionButtonStyle}"
                                            Margin="0,0,6,6"
                                            MinWidth="110" />
                                    <Button Content="Entfernen"
                                            Click="RemovePath_Click"
                                            Style="{StaticResource NormalActionButtonStyle}"
                                            Margin="0,0,6,6"
                                            MinWidth="110" />
                                    <Button Content="Ausgewählten Pfad verwenden"
                                            Click="UsePath_Click"
                                            Style="{StaticResource NormalActionButtonStyle}"
                                            Margin="0,0,6,6"
                                            MinWidth="190" />
                                    <Button Content="Auto-Erkennung"
                                            Click="AutoDetect_Click"
                                            Style="{StaticResource NormalActionButtonStyle}"
                                            Margin="0,0,6,6"
                                            MinWidth="130" />
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Background="{StaticResource ControlBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                Padding="14">
                            <StackPanel>
                                <TextBlock Text="PROTOKOLLIERUNG &amp; BACKUPS"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource GoldBrush}"
                                           Margin="0,0,0,8" />
                                <CheckBox x:Name="GeneralLogging"
                                          Margin="0,3" />
                                <CheckBox x:Name="DebugLogging"
                                          Margin="0,3" />
                                <CheckBox x:Name="SelectableBackups"
                                          Margin="0,3" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,2,0,0">
                <Button Content="Speichern"
                        Click="Save_Click"
                        Margin="6"
                        MinWidth="120"
                        Height="42"
                        Style="{StaticResource NormalActionButtonStyle}" />
                <Button Content="Abbrechen"
                        Click="Cancel_Click"
                        Margin="6"
                        MinWidth="120"
                        Height="42"
                        Style="{StaticResource NormalActionButtonStyle}" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
